import { ISuccessProps } from './success.types.ts';
import { FC } from 'react';
import { Box } from '@chakra-ui/react';

const Success: FC<ISuccessProps> = () => {
  return <Box>Success!</Box>;
};

export default Success;